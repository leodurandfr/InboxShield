# Database
DATABASE_URL=postgresql+asyncpg://inboxshield:changeme@localhost:5432/inboxshield

# Encryption key for IMAP credentials (Fernet)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Ollama (local LLM — default)
OLLAMA_BASE_URL=http://localhost:11434

# Cloud LLM API keys (optional — leave empty for local-only mode)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
MISTRAL_API_KEY=

# App
DEBUG=false
